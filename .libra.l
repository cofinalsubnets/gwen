; .libra.l -- the tree's own libra settings (lib/salt.l reads this over ~/.love/etc/libra.l).
; the gate is NO GRIPES: singleton (on by default) is strict here, and a gripe is
; inspected and fixed by hand -- never auto-applied.
(strict singleton)
