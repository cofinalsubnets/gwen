; lush 🐚, assembled: the shell state + job control, the tokenize/parse/execute
; engine, the repl loops, then the entry + seat -- all INCLUDED into this one
; layer (a slashed use evals into the current head), so (use 'lush) loads the
; lot and leave registers it whole.
(use 'lush/job)
(use 'lush/eval)
(use 'lush/line)
(use 'lush/main)
